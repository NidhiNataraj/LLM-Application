[project]
name = "llm-application"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nidhiswari N", email = "nidhinataraj06@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "jupyter>=1.1.1",
    "minsearch>=0.2.0",
    "openai>=3.8.0",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
llm-application = "llm_application:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
